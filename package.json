{
  "name": "anthbot-dashboard-card",
  "version": "0.3.0-dev.3",
  "private": true,
  "description": "Hero-style Home Assistant dashboard card for ANTHBOT Map",
  "scripts": {
    "check": "node --check anthbot-dashboard-card.js && node --check anthbot-dashboard-card-core.js && node tests/check-embedded-map-chrome.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
